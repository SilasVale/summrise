.mcp.json
.wrangler/
nohup.out
.claude/
gateway/public_backup/
屏幕截图*.png
index/public/summrise-agent/*.exe
index/public/summrise-agent/*.tgz
agent/summrise-agent-npm/*.exe
agent/summrise-agent-npm/*.tgz
agent/target-staging.exe

.summrise-tmp-*
.dsh-tmp-*

# any nested node_modules — the single rule now; the per-dir ones went with summrise-studio
**/node_modules/

# build artifacts — never commit
agent/deploy/cloudflared.exe
agent/deploy/summrise-playwright*.zip
agent/summrise-agent-npm/summrise-playwright.zip
agent/summrise-agent-npm/cloudflared.exe
agent/summrise-agent-npm/summrise-desktop.exe
agent/summrise-agent-npm/summrise-tray.exe
agent/summrise-agent-npm/bridge.js
agent/summrise-desktop/src-tauri/target/

# local tooling
.zcode/
cloudflared-run.sh

# personal dsh infra (machine-local, untracked; working copies stay on disk)
/dsh-wrapper.sh
/restart-plugin.js
/restart-plugin-client.js
/restart-plugin-host.js
/patch-dsh-trusted-host.sh
/scripts/restart-dsh.sh
/ecosystem.config.js

# The CDN-vs-GitHub reconcile ledger: one line per version published with no release to audit
# against, and DELIBERATELY MACHINE-LOCAL (scripts/lib/release-lib.sh says so where it defines
# the path). `git add -A` swept it into the 1.2.450 release commit on 2026-09-22, which put a
# live debt into every checkout and made `scripts/test/publish-release.bash` fail in CI — its
# scenarios got the reconcile refusal where they expect the stale-exe one. Ignored so the next
# `git add -A` cannot do it again.
docs/agents/release-reconcile.txt

# machine-local hook links (round 118) — the command is in AGENTS.md, not the link
.githooks/

# Worktrees for the branch-per-change flow (using-git-worktrees). MUST be ignored: an unignored
# worktree directory commits the whole tree into the repository. Measured 2026-09-26: it was NOT
# ignored, which is why this is landing 1's first task rather than an assumption.
.worktrees/
