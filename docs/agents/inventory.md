# Vale — what exists, measured

**Why this file.** The operator asked for a survey: what does Vale Agent have, what is needed, what can be merged, what
can be deleted. Every claim below is a measurement — a count from a command, a `path:line`, or a quoted header comment.
Where a verdict is a judgement rather than a measurement it says `[judgment]`. Nothing here was deleted or changed to
produce it: it is a read-only inventory taken on 2026-09-21, at `main` = `476c276a`.

**How to act on it.** §4 is the delete list, §3 the merge list, §5 defects the inventory itself found, §6 the questions
only the operator can answer. §2 is what must not be touched, with the reason.

---

## 1. The numbers

| area | size | evidence |
|---|---|---|
| `agent/src` (Rust) | **47,050 lines** | `find agent/src -name '*.rs' \| xargs wc -l` |
| ├ plugins | 17,197 lines / 30 files | terminal 5,771 · memory 3,869 · mcp_client 2,158 · update 1,576 · system 1,443 · playwright 1,123 · design 682 · monitor 213 · runs 134 |
| ├ tools (terminal backends) | 7,619 lines / 12 files | `agent/src/tools/` |
| └ web (HTTP + SSE + panel embed) | 7,869 lines / 4 files | `agent/src/web/` |
| MCP tools | **56** | `agent/spec-tools.json`, regenerated by `cargo test spec_snapshot` (`agent/src/web/mod.rs:2844`) |
| HTTP endpoints | **37 (method, path) shapes + `/mcp`** | 24 dispatch arms `agent/src/web/mod.rs:792-888`, 13 pre-dispatch shapes `:595-670` |
| plugins | **9**, all registered unconditionally (`agent/src/state.rs:80-100`) | no enable/disable flag exists |
| `vale` CLI | 12 subcommands, 2,983 lines | `agent/vale-agent-npm/src/vale.ts` |
| panel SPA | **38,375 lines**; 46 components (8,755) · 19 hooks (3,177) · 28 lib modules (3,051) | `find agent/resources/panel-react/src -type f \| xargs wc -l` |
| gateway | **13,724 lines / 41 files**, **61 route registrations** | `gateway/src`; routes registered by `plugins/registry.ts:123` |
| console UI | 5,312 lines / 20 files + `globals.css` 2,731; **6 views**, all mounted (`App.tsx:21-26`) | `gateway/ui/src` |
| landing | 531 lines / 29,496 bytes, one HTML string; **22 CSS classes defined, 22 used** | `index/src/page.js` |
| proxies | 3 workers (zen-go 403, zen-us 339, api-relay 2,256 lines) — **all three referenced**, none orphaned | `channels.ts:436,456-467`; `build.sh:305-335`; `ci.yml:647-671` |
| scripts | 46 files: 12 top level, 2 lib, 1 hook, **31 gates** (all CI-run) | `ls scripts scripts/test` |
| tests | Rust **757**, gateway **877** + console 34, panel **803**, index 14, proxies 98, CLI/shell suites | each suite's own runner; all in CI except the device-targeted ones (§5.8) |
| docs | 5 files: CHARTER 56, design-ledger 2,406, ideas 39, AGENTS 199, README 83 | `wc -l` |

MCP tools by plugin `[measured]`: terminal **27** · system **9** · memory **6** · mcp-client **4** · monitor **4** ·
playwright **2** · runs **2** · update **1** · design **1**.

---

## 2. KEEP — what earns its place

| item | why it stays |
|---|---|
| **the panel's page set** (terminal, history, browser, memory, plugins, settings) | every page renders in **both** densities, and that is a gate: `src/lib/pageCoverage.test.ts:53-67` parses the `Page` union out of `Shell.tsx` and requires a render site in both shells |
| **the four command renderers** (Trajectory / Path / CommandStream / DetailsPanel) | four projections of **one** read, not four implementations: one poll (`App.tsx:122-124` → `GET /api/sessions/{sid}` every 2 s), two groupers (`groupEvents` → cards, `groupRounds` → rounds), no view fetches its own (`useTrajectory.ts:98-100`). Deleting one removes a *shape*, not a duplicate — and CommandStream/DetailsPanel are the desktop density's only command inspector (§3.6) |
| **all 46 panel components** | **no dead component exists**: every one has a live JSX render site `[measured]` by grepping `<Name` over non-test `.tsx`; `lib/orphanModules.test.ts` enforces the module-level version and proves its own two exemptions earned |
| **all 10 `agent/tests/fixtures/*.json`** | **no orphan fixture**: each is read by a Rust `include_str!` **and** a panel test — the dual-ended wire contracts. Indexed as a route→fixture map at `lib/routeContracts.test.ts:24-59` |
| **all 31 gates in `scripts/test/`** | all CI-run; the overlap audit (§3.1) found exactly one redundant pair and no gate whose question is asked twice elsewhere |
| **the three design sweeps + `panel-render-audit.mjs`** | one shared core (`lib/design-sweep.mjs` 1,594 lines + `lib/contrast-probe.mjs` 549); each adapter supplies only its URL, page list, fixtures and widths |
| **the marks / liveness / path state unions** | one declaration each: `PATH_STATES` (`lib/path.ts:36`), the liveness model (`lib/liveness.ts`), `cardState` as the canonical deriver (`CommandCard.tsx:33-52`) — the last one with a comment recording what happened when it was restated |
| **the 15 MCP tools with no in-repo caller** | **not dead**: they are reachable through the device's own `/mcp` endpoint (`ConnectCard.tsx:119`), which is how external AI clients use them, and 13 carry an explicit `NOT_EXPOSED` reason in the gateway (`gateway/test/mcp-handler.test.mjs:260-296`). See §6.1 for the one question this raises |

---

## 3. MERGE — the same job in two places

### 3.1 `custom-prop-check.mjs` ⊂ `css-vars-check.mjs` (panel + console) `[measured]`
Both build a `defined` set of CSS custom properties and diff it against `var()` uses. Measured set sizes on this
checkout: **custom-prop 115, css-vars 114 — the difference is `--amber-bright`,** which occurs **only inside a comment**
(`agent/resources/panel-react/src/styles/layout.css:100`, `/* --warn-ink, not --amber-bright: … */`).

`css-vars-check.mjs:78-84` records this exact defect and fixes it ("COMMENTS ARE NOT DECLARATIONS"); `custom-prop-check.mjs:62-65`
still counts raw text. Consequence: the older gate would accept `color: var(--amber-bright)` as resolved — a silent false
pass — where the newer one fails it.

**Neither is disposable:** custom-prop also covers the **landing** (css-vars covers panel+console only,
`css-vars-check.mjs:26-29`), and css-vars adds the reverse direction (declared-but-unread, which found 21 tokens) plus
runtime `getPropertyValue` reads. **Merge:** strip comments in custom-prop, or hand it the landing slice alone and let
css-vars own panel+console.

### 3.2 `AGENTS.md` vs `agent/AGENTS.md` — the release procedure written twice `[measured]`
Diffed section by section: **Release 28 of 30 lines identical (93 %)**, Build 5/14 (35 %), Test 6/76 (7 %). The release
recipe (bump → build → cp exe → publish → one commit → tag via API → audit) is stated twice, near-verbatim.
**Merge:** keep one copy, point the other at it.

### 3.3 `DesktopShell`'s footer chip row, written twice in one file `[measured]`
`DesktopShell.tsx:520-527` (`showStatus`) and `:543-550` (`!showStatus`) render the same `BootChip`/`LoadChip`/`MonitorChip`/
`WaitingChip` with the same props — byte-identical apart from indentation. The two `className="desktop-status-msg"` spans
(`:512`, `:538`) are the same status line twice. **Merge:** hoist the row; let the boolean choose the wrapper only.

### 3.4 `TerminalWorkspace`'s two density branches `[measured]`
Desktop `:225-279` and panel `:280-381`: the `PathView`/`TrajectoryView`/`TerminalPane` mounts are copied
character-for-character (`:235-277` vs `:307-346`); the desktop branch adds only the container class and
`controlledView`. ~40 duplicated lines, two places to forget a prop — and the file's own comment at `:132-134` names
this exact trap ("a per-density copy is how the two drifted before (R131)"). **Merge:** one body, a density-aware
wrapper.

### 3.5 `useSessions.ts`'s session-row mapper, twice in one file `[measured]`
`:228` and `:329` are byte-identical after whitespace-trim (verified with `diff`) — the device session-row → `Session`
mapper, once in the event-driven tick and once in the 30-second sweep. `last_exit_code` and `idle_ms` each appear 5× in
that file. **Merge:** one function.

### 3.6 `PAGE_LABELS` vs `PAGE_TITLES` `[measured]`
`Shell.tsx:17-25` and `DesktopShell.tsx:109-116` carry the same six strings. The same file shows the fix already applied
to the icon map: `DesktopShell.tsx:32` **imports** `PAGE_ICONS`. **Merge:** import `PAGE_LABELS`. **Do not delete
`PAGE_LABELS`** — it is a test-only seam, which this repo counts as use (`scripts/test/exports-check.mjs:25`).

### 3.7 The update verdict is computed twice, and the two can disagree `[measured]`
The **same fact** ("is this device behind?") is derived in two places from two relays:
`agent/src/plugins/update/tools.rs:280-315` answers `GET /api/update` with `{current, latest, update_available}` — the
panel's `UpdateCard.tsx:143` renders that verbatim, pin-aware. The console does not read `/api/update` at all: it
computes `outdated = d.lastVersion !== install.version` (`DevicesPanel.tsx:349`), where `install.version` comes from
`/api/devices/install-cmd` → `agent.saisi.online/api/version` (`devices.ts:663`).

Both ultimately read the same published `version.json`, but the comparisons are different code: the device's uses
`newer()` plus the rollback pin, the console's is a string `!==` over a KV value written **at most hourly**
(`store/devices.ts:149`). So a pinned or just-updated device can show "current" in the panel and "update available" in
the console — and the panel already knows the better answer. **Merge:** have the console ask the device (it already
proxies to it) or have the gateway expose the device's own verdict, rather than re-deriving it from a lagging copy.

### 3.8 The crash verdict: the gateway forwards one value, the console tests it inline `[measured]`
`last_boot_kind` is a **five-value union** on the device, and the panel parses all five
(`useAgentVitals.ts:35-64` → `BootChip` + `lib/bootNotice.ts` rules, plus `/api/boots` for 24 h of history). The
gateway forwards **only `crashed`** (`gateway/src/mcp.ts:100-107`), and the console then matches the literal inline:
`st?.last_boot_kind === "crashed"` (`DevicesPanel.tsx:362-363`). The two agree on a crash and the console is blind to
`clean-exit` / `replaced` / `machine-restart` **by design** — but the design is encoded in a string comparison inside a
component, which is the shape this repo has already fixed twice (rounds 96-100). **Merge:** one parsed union on the
relayed payload, and let the console decide what it shows.

### 3.9 Four renderers over one array: already merged, keep it that way `[measured]`
Recorded here because it is the thing a future prune would get wrong: the four command views share one poll and two
groupers. The only genuine duplication in that area is a **fetch** (§5.2).

---

## 4. DELETE — nothing in the repository needs it

| # | item | evidence it is unneeded | risk |
|---|---|---|---|
| 4.1 | `GET /api/events` (SSE) | **no client anywhere in the repo** `[measured]`: the panel's own fetch was removed (`EvidenceDrawer.tsx:8-9`); the **built** `panel.js` has 0 matches for `/api/events"` | an out-of-repo client (older panel build, a phone shortcut) would break — see §6.4 |
| 4.2 | `GET /api/events/poll` | **no client anywhere** `[measured]`; its only documented unique value is the epoch nonce (`sse.rs:138`) | same as 4.1 |
| 4.3 | `scripts/live-panel-probe.mjs` (91 lines) | **zero referrers in the whole repository** `[measured]` — no script, hook or workflow runs it. It is also the only instrument that measures the **live** panel | it is cheap to keep once wired (see §5.5); deleting it removes the only live-panel instrument |
| 4.4 | `README.md`'s three dead links + CHARTER's two | verified with `test -e`: `README.md:47` → `docs/adr/0006`; `:51` → `docs/adr/` and `research/superpowers`; `:70` → `docs/superpowers/specs/2026-08-28-…md`; `docs/CHARTER.md:5` → `docs/agents/iteration-loop.md`; `:53` → "the journal" | **CHARTER is the operator's file** (`ideas.md` row 11 files it) — the loop may only propose |
| 4.5 | the two READMEs' tool count | they advertise "49 MCP tools" and terminal 26; **measured 56 and 27**, and `monitor` (4) + `runs` (2) are missing from the lists entirely | a published number may be contractual for the npm listing (§6.6) |
| 4.6 | `scripts/lib/release-lib.sh:148`'s comment | calls `docs/agents/release-reconcile.txt` "TRACKED in git"; it is not tracked and does not exist. The tolerance is coded, so it is a false comment, not a failure | — |
| 4.7 | **three gateway routes with no in-repo client**: `POST`/`DELETE /api/me/token/relay`, `POST /api/me/token/relay/reveal` (`auth.ts:802-804`), `POST /api/register` (`devices.ts:772`), and `ANY /api/devices/{name}/proxy/*` (`devices.ts:778`, kept since the extension was deleted — `device-proxy.ts:209` says "an admin visiting it directly") | `[measured]`: no console type declares `relayTokenSet` (`client.ts:119-124`); `/api/register` has only comments and tests; nothing builds a proxy URL | these are credential-lifecycle and installer-compat routes — §6.11 |
| 4.8 | `build.sh:328-329`'s "round-320: `build-installer.sh` retired … it always failed" | the file exists, and `publish-release.sh:344` + `publish-cdn-from-ci.sh:101` **call it**, and the landing's Setup.exe button depends on its output (`index.js:616-636`) | the comment is false; either the script or the comment goes — §6.12 |
| 4.9 | `AGENTS.md:62`'s gate count | says `panel-design-sweep.bash` has "51 checks"/"52 since round 100"; **measured 79 ok, 0 failed**. The axis list beside it is accurate | — |

---

## 5. FIX — defects this inventory measured (not deletions)

| # | defect | measurement |
|---|---|---|
| 5.1 | **One fact, two states, in one file.** `TrajectoryView.eventDotState` (`:68-76`) is a second private deriver over the same command fields as `cardState`; it has no `bg` in its union and maps `status === "backgrounded"` to **`warn`** (`:71`) — the exact mapping `CommandCard.tsx:41-46` records as the defect it fixed. So one backgrounded command's *round* marker reads `bg` while its own *event row* reads `warn`. Untested: no "backgrounded" in `TrajectoryView.test.tsx` | `[measured]` grep + the two source sites |
| 5.2 | **A second poll of the same session.** `ArchivePage.tsx:112` mounts its own `useCommandEvents(entry.sid)` while `App.tsx:122` polls the active sid **unconditionally** (it sits above the shell branch). History + expanding the row of the active session = 2× `GET /api/sessions/{same sid}` every 2 s | `[measured]` |
| 5.3 | **The desktop tab strip's accessible name is ambiguous again.** `DesktopShell.tsx:303-312` sets `title`/`aria-label` from the **raw** label while the visible text is the disambiguated `shown` (`:326`); `TabBar.tsx:99-106` uses the disambiguated label in both. For the 10×`pwsh` case that `lib/sessionLabels.ts` exists for, the screen-reader name lost the fix | `[measured]` |
| 5.4 | **The ratchet has a gap.** `lib/sessionFacts.test.ts` forbids a component from reading four session fields (`pendingApproval`, `idleMs`, `commandRunning`, `heldByHuman`) and scans every `.tsx`. Command facts (`exitCode`, `reason`, `ended`) are **not** on that list — which is why 5.1 and `ActivityPage.tsx:98`'s inline `row.exitCode === 0 ? …` pass the gate | `[measured]` |
| 5.5 | **The pre-commit hook misses two of five emitters.** It covers panel, console, `panel-render-audit`, `contrast-probe-check`; `landing-design-sweep.mjs` (a real emitter, 72 KB script) and `live-panel-probe.mjs` (26 KB, checked by nothing) are not in the loop. CI catches the landing one **after a push** — the cost the hook exists to avoid. The hook is also still not installed (`ideas.md` row 13) | `[measured]`, hook measured at 0.49 s total |
| 5.6 | **A gate that can silently not run.** `ci.yml:414-420` runs the installer-logic tests (`ValeIntegrity.tests.ps1`) only `if command -v pwsh`, else prints a `::warning::` and continues — a skip that reads as a pass | `[measured]` |
| 5.7 | **A gate that blames the wrong thing.** `scripts/test/publish-release.bash:28-30` requires a clean worktree and reports any dirt as "the refusal modified the tree" — it failed on this checkout because of an uncommitted `docs/agents/ideas.md` edit of mine. It passes in CI (clean checkout). Fix: snapshot before / compare after | `[measured]` |
| 5.8 | **Seven e2e sections never run in CI** (`agent/scripts/e2e/e2e.js`: terminal, file, workflow, panel, mcp, evidence, browser). By design they are device-targeted (`ci.yml:185-188`), but nothing else runs them either — no schedule, no documented manual cadence | `[measured]` |

---

## 6. NEEDS YOU — what the repository cannot answer

1. **Do external MCP clients use the 15 no-caller tools** (`system_file_read/write/list/stat`, `system_process_*`,
   `system_net_test`, `page_view`, `mcp_client_list/disconnect`, `sftp`/`terminal_sftp`, the prefixed `terminal_secret_*`)?
   The device serves them on its own `/mcp`, and the repo cannot see that traffic. §2 keeps them on that basis.
2. **Which name wins for secrets** — the canonical `terminal_secret_*` or the "legacy" `secret_*` the gateway exposes?
   One rename decides which clients break (`terminal/tools/mod.rs:83-85`).
3. **Is `page_view`'s premise still true?** Its header says "the device has no browser, so 'seeing' the design means
   reading the source" — while the device **bundles Playwright** and `browser_run_script` exists. `[judgment]`
4. **Are `/api/events` and `/api/events/poll` retained for an out-of-repo client**, or are they dead (§4.1-4.2)?
5. **Does `agent_update` belong on the console MCP surface?** `NOT_EXPOSED` says no; the panel calls it anyway
   (`UpdateCard.tsx:205`).
6. **Are the README tool counts contractual?** (§4.5)
7. **Is the panel density (browser) still used, or is the desktop window the only real front end?** It matters because
   the desktop density has **no command-card stream and no details inspector** — the panel-only cluster is
   `TabBar`, `CommandStream`, `CommandCard`, `CopyButton`, `DetailsPanel` `[measured]`. That is the largest functional
   asymmetry between the two densities: either the desktop gets them, or the panel's browser density is the "full" one
   on purpose.
8. **Should `CHARTER`'s two dead citations be fixed** (§4.4)? It is the operator's file; `ideas.md` row 11 already
   asks.
9. **Should the seven unused e2e sections get a cadence** (a nightly workflow, or an explicit "run by hand before a
   release" line in AGENTS.md)?
10. **Is any out-of-repo client still minting relay tokens?** (`/api/me/token/relay*`) The AGENTS.md transport rules
    assume that lifecycle; the console has no UI for it. If nothing uses it, three routes and the `users.ts` relay
    columns are prunable.
11. **Are retired installers still in the field calling `POST /api/register`**, or is `vale setup --reg-key` →
    `/api/devices/self-register` the only path now?
12. **Keep or retire `/api/devices/{name}/proxy/*`** — reachable, tested, and nothing constructs the URL any more.
13. **Should the console's version badge carry the device's own verdict** (§3.7), or is "what the CDN publishes vs what
    the KV last saw" deliberately a different question?
14. **`build-installer.sh`: script or comment?** (§4.8)
15. **Are five kept tarballs worth ~35 MB** in `index/public/vale-agent/` (1.2.432–437), when only `latest.tgz` is
    linked from the landing?
16. **The six inbox rows still "waiting on you"** (`ideas.md`: 11, 12, 13, 17, 18, 22) — of these, row 17 (one shape,
    two meanings) and row 22 (the failed-session mark) are design decisions this inventory can measure but not take.

---

## 7. The operator's questions, after going and looking

Every item below was "waiting on you" in §6. Most of them turned out to be answerable by measurement, and two turned
out to be answerable only after a small change to the device. Measured 2026-09-21 against d1 (running 1.2.437) and the
live gateway.

| question | what the machine says |
|---|---|
| **1. Do external MCP clients call the 15 no-caller tools?** | **Not answerable until today, and that was the real finding.** d1's `mcp_diag.log` holds **320 `tools/call` lines and not one tool name** — every line came from the OUTGOING `mcp_client` bridge (the device calling playwright-mcp); the server external clients call recorded **nothing**. FIXED this round: `agent/src/mcp/server.rs` now writes `[call] tool=<name> ms=<n> ok\|error=…` to the same 1 MB-capped rotated file, arguments deliberately excluded (they carry credentials). The next release answers the question from traffic. `[measured]` |
| **2. Which secret name wins?** | The gateway exposes the **bare** names (`secret_set/get/delete`) and marks the prefixed ones as aliases; the device registers both, with `terminal_secret_*` documented as canonical (`terminal/tools/mod.rs:83-85`). So the canonical name is the one no gateway client can reach. Recommendation: make the bare pair canonical (they are what the console and the MCP surface already use) rather than renaming clients. `[measured]` |
| **3. Is `page_view`'s "the device has no browser" premise still true?** | **No.** d1's own log records `playwright auto-start: {"port":9229,"status":"started"}` at boot, the binaries live in `D:\Vale\components`, and `browser_run_script` is a live console tool. The premise in `design/tools.rs:135` is stale; `page_view` may still earn its place (it reads a page's SOURCE, which a browser does not hand you), but not for that reason. `[measured]` |
| **4. Are `/api/events` and `/api/events/poll` retained for an out-of-repo client?** | Nothing in the repo reads them (the built bundle has zero references, the panel's own fetch was removed). The live usage cannot be measured — the agent keeps no HTTP access log — but the failure mode of deleting them is a **visible 404**, not a silent one. `[measured]` for the repo side, `[judgment]` for the risk |
| **5. Does the console's version badge disagree with the device?** | **Not today, and here is exactly when it would.** The device reports `update_available:false, current:1.2.437, latest:1.2.437, pinned_to:null` with a fresh `checked_at`; the console's formula (`lastVersion !== install.version`) agrees because both sides read 1.2.437. They diverge when a rollback pin is set (the console ignores `pinned_to`) or when the KV `lastVersion` is up to an hour stale. `[measured]` |
| **6. Is the browser density still used?** | **Yes, rarely, and the device can prove it**: `panel-grants-redeemed.txt` holds exactly **one** redeemed grant (`1789867510 45a705f3…`) — the console's "open panel" button mints those — and the desktop window is what an operator looks at day to day. One use is enough to keep the route; it is not enough to argue the desktop density should stay the poorer one. `[measured]` |
| **7. `build-installer.sh`: script or comment?** | Settled: **the script is alive.** `182a0347` deleted it ("dead since the CDN rework"), `cf6b3383` restored it ("one setup.exe over the npm channel"), and `publish-release.sh:344` calls it on every release. The three comments in `build.sh` that called it retired are corrected in this round. What is true instead: the manifest currently carries no `installer` field, so the landing shows no Setup.exe button — a publication state. `[measured]` |
| **8. Are five kept tarballs worth ~35 MB?** | The directory holds 1.2.432/434/435/436/437 (~6.9 MB each) while only `latest.tgz` is linked from the landing. They are what `vale rollback <x.y.z>` pins to, so the policy has a user; 1.2.432 predates every release this session made. `[measured]` |

### Two things the device itself was carrying

| finding | measurement |
|---|---|
| `D:\Vale\vale-agent.yaml.bad` — **17,140,736 bytes whose first two bytes are `4D 5A` ("MZ")**: it is a Windows EXECUTABLE named as if it were a config file, dated 2026-09-10, and nothing has cleaned it up. Whatever quarantined it wrote a name that lies about what it holds. | `[measured]` on d1 |
| `C:\ProgramData\Vale\mcp_diag.log` (168,620 bytes) sits at the **pre-layout-v2** location while the live writer uses `C:\ProgramData\Vale\logs\mcp_diag.log` — a migration leftover that will never be appended again. | `[measured]` on d1 |
