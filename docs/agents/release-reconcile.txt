# Versions on the CDN with no GitHub release to audit against.
# Written by scripts/publish-release.sh --skip-reconcile; cleared by --audit-only <ver>.
#
# MEASURED 2026-09-14 (round 122), which is why this file exists at all: the newest
# GitHub release in SilasVale/vale was v1.2.361 while the CDN served 1.2.361–1.2.364,
# so three shipped versions had no release, no tag and therefore no dual-builder audit
# — and nothing anywhere said so. v1.2.361 is NOT listed: its release exists (HTTP 200)
# and its audit can run.
1.2.362 2026-09-14T00:00:00Z backfilled by round 123 from the live state: CDN 200 / GitHub release 404
1.2.363 2026-09-14T00:00:00Z backfilled by round 123 from the live state: CDN 200 / GitHub release 404
1.2.364 2026-09-14T00:00:00Z backfilled by round 123 from the live state: CDN 200 / GitHub release 404
1.2.370 2026-09-14T10:30:00Z recorded by round 259 from the live state: CDN 200 (tgz still served) / GitHub release 404. SUPERSEDED WITHIN ITS OWN ROUND — 1.2.371 shipped ~20 minutes later with the same panel-update feature plus the serial fix, and 1.2.370's commit (27924b9e) has NO CI run for release.yml's tag gate to check, so its tag cannot be created without failing that gate by design. Its code is byte-identical to 1.2.371's apart from the version string, and 1.2.371 + 1.2.372 both audited byte-for-byte. Listed rather than omitted: an unreconciled publish is a fact, and the point of this file is that it is never silent.
1.2.374 2026-09-14T11:35:00Z recorded by round 261 from the live state: CDN 200 (tgz still served) / GitHub release 404. SUPERSEDED WITHIN ITS OWN ROUND by 1.2.375 (same reachability feature plus one wording fix), and its commits cannot pass release.yml's tag gate: 89b9fe80 is RED (the monitors test fixtures the panel's own tsc refused and the module-map entries it demanded), and 8f1f419b — the fix — carries NO CI run because CI only runs on a pushed tip. Listed rather than omitted, with the reason.
1.2.378 2026-09-14T13:47:00Z recorded by round 264 from the live state: CDN 200 (tgz still served) / GitHub release 404. SUPERSEDED WITHIN ITS OWN ROUND by 1.2.379 (which carries the feature AND the fix the DEVICE found — two notifications for one outage), and its commits cannot pass release.yml's tag gate: 8fef06b8 is RED on `the_ledgers_headline_scope_matches_what_the_tool_measures` — the coverage headline went stale the moment the feature commit added files, and the ledger update landed in a LATER commit. **The process lesson, recorded here because it has now cost three rounds: update the ledger's numbers in the SAME commit that changes the file set.** Listed rather than omitted.
