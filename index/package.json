{
  "name": "summrise-dist",
  "private": true,
  "type": "module",
  "description": "Summrise Index worker (Cloudflare) — one-time-claim unit tests via node --test",
  "scripts": {
    "test": "node --test"
  }
}
